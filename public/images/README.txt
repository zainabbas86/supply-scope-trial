Portfolio assets.

zain.jpg - the header photo on the portfolio page (resources/js/pages/Portfolio.tsx).
Square-ish crop, 512x512 is plenty; it renders at 112x112 CSS pixels.
Until it exists the page falls back to initials rather than a broken image.
